---
const { title, url, date } = Astro.props;
---

<li class="pb-2">
  <a
    href={url}
    class="flex justify-between font-medium hover:bg-primary hover:text-darkPrimary transition-colors duration-300 px-1"
  >
    <h3>{title}</h3>
    <time class="hidden lg:block pr-2"
      >{date.toLocaleDateString().replace(/\//g, "-")}</time
    >
  </a>
</li>
